{
  "name": "sepiola",
  "version": "0.1.0",
  "private": true,
  "description": "Sepiola \u2014 a WebMCP telestrator: draws CHIRP's fantasy-hockey reads onto a persistent screen. The analyst decides, the agent draws, the screen has no opinions.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "scenarios": "playwright test",
    "build": "vite build",
    "docs:grammar": "node scripts/grammar-doc.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name sepiola",
    "brand:card": "node scripts/social-card.mjs"
  },
  "dependencies": {
    "gsap": "3.15.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "vite": "8.2.2",
    "vite-plugin-singlefile": "2.3.3",
    "vitest": "5.0.0",
    "wrangler": "4.129.0"
  }
}
